# VaxRecord Environment Variables
# Copy this file to .env and fill in your values

# OpenAI API Key (Required for vaccine parsing)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Database URL (Optional - only needed if using database features)
# For local development, you can use a Neon Postgres database or local Postgres
# DATABASE_URL=postgresql://user:password@localhost:5432/vaxrecord
# Or use Neon: DATABASE_URL=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/dbname

# Node Environment
NODE_ENV=development

# Server Port (default: 8080)
PORT=8080

# AWS Region (for Amplify deployment)
# AWS_REGION=us-east-1

# CORS Allowed Origin (for production deployment)
# Set this to your frontend domain in production
# ALLOWED_ORIGIN=https://your-frontend-domain.amplifyapp.com

# Frontend API URL (for frontend builds)
# VITE_API_URL=http://localhost:8080
