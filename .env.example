# VaxRecord Environment Variables Example
# Copy this file to .env and fill in your actual values

# Application Environment
NODE_ENV=development
PORT=8080

# OpenAI API Configuration (Required for vaccine parsing)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Frontend API URL (Vite)
VITE_API_URL=http://localhost:8080

# Backend CORS Configuration
# Set to your Amplify frontend domain in production
ALLOWED_ORIGIN=*

# Optional: Database (currently not used in Lambda deployment)
# DATABASE_URL=postgresql://username:password@host:5432/database?sslmode=require

# Optional: Logging
# LOG_LEVEL=info
